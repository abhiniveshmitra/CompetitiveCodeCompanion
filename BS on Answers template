bool feasible(vector<lli>& a, lli k,lli n,lli mid)
{

}
 lli bs(vector<lli>& a, vector<lli>& b,lli n)
{
    lli low=1;
    lli high=n;
    lli ans=low;
    while(low<=high)
    {
        lli mid=(low+high)/2;
        if(feasible(a,b,n,mid,poorer,richer))
        {
            ans=mid;
            low=mid+1;
        }
        else high=mid-1;
    }
    return ans;
}
   
